import { createStyles } from '@mantine/core'

export const useAboutPageStyles = createStyles({
  container: {
    maxWidth: '55rem',
    marginLeft: 'auto',
    marginRight: 'auto',
    padding: '0 1rem',
  },
  back: {
    cursor: 'pointer',
  },
})
